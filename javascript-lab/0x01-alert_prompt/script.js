// Adding two numbers in JS

const number1 = parseInt(prompt('Enter First Number, Defualt is 0', 0));
const number2 = parseInt(prompt('Enter second Number, Default is 0', 0));

const sum = number1 + number2;

alert(`${number1} + ${number2} = ${sum}`);
